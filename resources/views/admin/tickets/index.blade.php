<x-layouts.panel title="Destek Talepleri"><h1 class="mb-4 text-2xl font-bold">Destek Talepleri</h1><div class="panel">@foreach($tickets as $ticket)<a class="block border-b border-slate-100 py-3 dark:border-slate-800" href="{{ route('admin.tickets.show',$ticket) }}">{{ $ticket->number }} - {{ $ticket->subject }} - {{ $ticket->user->name }} <span class="badge">{{ \App\Support\Status::label($ticket->status) }}</span></a>@endforeach{{ $tickets->links() }}</div></x-layouts.panel>
