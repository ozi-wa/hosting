<x-layouts.panel title="Fatura"><div class="panel"><h1 class="text-2xl font-bold">{{ $invoice->number }}</h1><p class="mt-2">{{ $invoice->user->name }} <span class="badge">{{ \App\Support\Status::label($invoice->status) }}</span></p><p class="mt-4 text-xl font-bold">{{ $invoice->total }} {{ $invoice->currency }}</p>@if($invoice->status !== 'paid')<form method="POST" action="{{ route('admin.invoices.mark-paid',$invoice) }}" class="mt-5">@csrf<button class="btn-primary">Manuel Ödendi İşaretle</button></form>@endif</div></x-layouts.panel>
