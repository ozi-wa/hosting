<x-layouts.panel title="Faturalar"><h1 class="mb-4 text-2xl font-bold">Faturalar</h1><div class="panel overflow-x-auto"><table class="w-full"><thead class="table-head"><tr><th class="p-4">Numara</th><th>Kullanıcı</th><th>Durum</th><th>Tutar</th></tr></thead><tbody>@foreach($invoices as $invoice)<tr><td class="table-cell"><a href="{{ route('admin.invoices.show',$invoice) }}">{{ $invoice->number }}</a></td><td class="table-cell">{{ $invoice->user->name }}</td><td class="table-cell">{{ \App\Support\Status::label($invoice->status) }}</td><td class="table-cell">{{ $invoice->total }}</td></tr>@endforeach</tbody></table>{{ $invoices->links() }}</div></x-layouts.panel>
