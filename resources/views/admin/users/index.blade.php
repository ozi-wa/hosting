<x-layouts.panel title="Kullanıcılar"><h1 class="mb-4 text-2xl font-bold">Kullanıcılar</h1><div class="panel overflow-x-auto"><table class="w-full"><thead class="table-head"><tr><th class="p-4">Ad</th><th>E-posta</th><th>Rol</th><th>Durum</th></tr></thead><tbody>@foreach($users as $user)<tr><td class="table-cell"><a href="{{ route('admin.users.edit',$user) }}">{{ $user->name }}</a></td><td class="table-cell">{{ $user->email }}</td><td class="table-cell">{{ \App\Support\Status::role($user->role) }}</td><td class="table-cell">{{ \App\Support\Status::label($user->status) }}</td></tr>@endforeach</tbody></table>{{ $users->links() }}</div></x-layouts.panel>
