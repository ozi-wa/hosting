<x-layouts.panel title="Ürün"><form method="POST" action="{{ $product->exists ? route('admin.products.update',$product) : route('admin.products.store') }}" class="panel grid gap-4 max-w-3xl">@csrf @if($product->exists) @method('PUT') @endif<h1 class="text-2xl font-bold">{{ $product->exists ? 'Ürün Düzenle' : 'Ürün Oluştur' }}</h1><label><span class="label">Kategori</span><select class="field" name="category_id">@foreach($categories as $category)<option value="{{ $category->id }}" @selected($product->category_id===$category->id)>{{ $category->name }}</option>@endforeach</select></label><div class="grid gap-4 md:grid-cols-2"><label><span class="label">Ad</span><input class="field" name="name" value="{{ old('name',$product->name) }}"></label><label><span class="label">Slug</span><input class="field" name="slug" value="{{ old('slug',$product->slug) }}"></label><label><span class="label">SKU</span><input class="field" name="sku" value="{{ old('sku',$product->sku) }}"></label><label><span class="label">WHMCS Ürün ID</span><input class="field" name="whmcs_product_id" type="number" value="{{ old('whmcs_product_id',$product->whmcs_product_id) }}"></label><label><span class="label">WHMCS Grup ID</span><input class="field" name="whmcs_gid" type="number" value="{{ old('whmcs_gid',$product->whmcs_gid) }}"></label><label><span class="label">Para birimi</span><input class="field" name="currency" value="{{ old('currency',$product->currency ?: 'TRY') }}"></label><label><span class="label">Aylık fiyat</span><input class="field" name="monthly_price" type="number" step="0.01" value="{{ old('monthly_price',$product->monthly_price) }}"></label><label><span class="label">Yıllık fiyat</span><input class="field" name="yearly_price" type="number" step="0.01" value="{{ old('yearly_price',$product->yearly_price) }}"></label></div><label><span class="label">Kısa açıklama</span><input class="field" name="short_description" value="{{ old('short_description',$product->short_description) }}"></label><label><span class="label">Açıklama</span><textarea class="field" name="description">{{ old('description',$product->description) }}</textarea></label><label><span class="label">Özellikler, her satıra bir özellik</span><textarea class="field" name="features">{{ old('features', implode("\n", $product->features ?? [])) }}</textarea></label><label class="flex gap-2 text-sm"><input type="checkbox" name="is_featured" value="1" @checked($product->is_featured)> Öne çıkan</label><label class="flex gap-2 text-sm"><input type="checkbox" name="is_active" value="1" @checked($product->is_active || ! $product->exists)> Aktif</label><button class="btn-primary w-fit">Kaydet</button></form></x-layouts.panel>
