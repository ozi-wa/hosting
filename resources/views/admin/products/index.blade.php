<x-layouts.panel title="Ürünler"><div class="mb-4 flex justify-between"><h1 class="text-2xl font-bold">Ürünler</h1><a class="btn-primary" href="{{ route('admin.products.create') }}">Yeni Ürün</a></div><div class="panel overflow-x-auto"><table class="w-full"><thead class="table-head"><tr><th class="p-4">Ad</th><th>Kategori</th><th>Fiyat</th><th>Durum</th></tr></thead><tbody>@foreach($products as $product)<tr><td class="table-cell"><a href="{{ route('admin.products.edit',$product) }}">{{ $product->name }}</a></td><td class="table-cell">{{ $product->category->name }}</td><td class="table-cell">{{ $product->monthly_price }} {{ $product->currency }}</td><td class="table-cell">{{ $product->is_active ? 'Aktif' : 'Pasif' }}</td></tr>@endforeach</tbody></table>{{ $products->links() }}</div></x-layouts.panel>
