<x-layouts.panel title="Sipariş"><div class="panel"><h1 class="text-2xl font-bold">{{ $order->number }}</h1><p class="mt-2">{{ $order->user->name }} - {{ $order->product->name }}</p><form method="POST" action="{{ route('admin.orders.update',$order) }}" class="mt-5 flex gap-3">@csrf @method('PUT')<select class="field max-w-xs" name="status">@foreach(['pending_payment'=>'Ödeme bekliyor','processing'=>'İşleniyor','active'=>'Aktif','cancelled'=>'İptal edildi','failed'=>'Başarısız'] as $status=>$label)<option value="{{ $status }}" @selected($order->status===$status)>{{ $label }}</option>@endforeach</select><button class="btn-primary">Kaydet</button></form></div></x-layouts.panel>
