<x-layouts.panel title="Blog"><div class="mb-4 flex justify-between"><h1 class="text-2xl font-bold">Blog Yazıları</h1><a class="btn-primary" href="{{ route('admin.blog-posts.create') }}">Yeni Yazı</a></div><div class="panel">@foreach($posts as $post)<a class="block border-b border-slate-100 py-3 dark:border-slate-800" href="{{ route('admin.blog-posts.edit',$post) }}">{{ $post->title }} <span class="badge">{{ \App\Support\Status::label($post->status) }}</span></a>@endforeach{{ $posts->links() }}</div></x-layouts.panel>
