<x-layouts.panel title="Fatura"><div class="panel"><h1 class="text-2xl font-bold">{{ $invoice->number }}</h1><p class="mt-2"><span class="badge">{{ \App\Support\Status::label($invoice->status) }}</span></p><div class="mt-6 grid gap-2 text-sm">@foreach(($invoice->line_items ?? []) as $item)<div class="flex justify-between border-b border-slate-100 py-2 dark:border-slate-800"><span>{{ $item['description'] }}</span><span>{{ $item['total'] }} {{ $invoice->currency }}</span></div>@endforeach</div><p class="mt-4 text-xl font-bold">Toplam: {{ $invoice->total }} {{ $invoice->currency }}</p><p class="mt-2 text-sm text-slate-500">Ödeme ve fatura akışı WHMCS üzerinden yönetilir.</p></div></x-layouts.panel>
