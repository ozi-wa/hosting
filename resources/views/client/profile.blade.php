<x-layouts.panel title="Profil"><form method="POST" action="{{ route('client.profile.update') }}" class="panel grid gap-4 max-w-2xl">@csrf @method('PUT')<h1 class="text-2xl font-bold">Profil</h1>@foreach(['name'=>'Ad Soyad','company_name'=>'Firma','phone'=>'Telefon','tax_number'=>'Vergi numarası'] as $field=>$label)<label><span class="label">{{ $label }}</span><input class="field" name="{{ $field }}" value="{{ old($field,$user->$field) }}"></label>@endforeach<label><span class="label">Fatura adresi</span><textarea class="field" name="billing_address">{{ old('billing_address',$user->billing_address) }}</textarea></label><button class="btn-primary w-fit">Kaydet</button></form></x-layouts.panel>
