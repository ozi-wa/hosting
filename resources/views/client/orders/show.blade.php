<x-layouts.panel title="Sipariş"><div class="panel"><h1 class="text-2xl font-bold">{{ $order->number }}</h1><p class="mt-3">{{ $order->product->name }} <span class="badge">{{ \App\Support\Status::label($order->status) }}</span></p><p class="mt-2 text-sm text-slate-500">Tutar: {{ $order->total }} {{ $order->currency }}</p>@if($order->invoice)<a class="btn-primary mt-5" href="{{ route('client.invoices.show',$order->invoice) }}">Faturayı Görüntüle</a>@endif</div></x-layouts.panel>
