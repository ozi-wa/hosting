<x-layouts.panel title="Destek Talebi"><div class="panel"><h1 class="text-2xl font-bold">{{ $ticket->subject }}</h1><p class="mt-2"><span class="badge">{{ \App\Support\Status::label($ticket->status) }}</span></p><div class="mt-6 grid gap-4">@foreach($ticket->messages as $message)<div class="rounded-md bg-slate-50 p-4 dark:bg-slate-950"><p class="text-sm font-semibold">{{ $message->user->name }} @if($message->is_staff_reply)<span class="badge">Yetkili</span>@endif</p><p class="mt-2 text-sm">{{ $message->message }}</p></div>@endforeach</div><form method="POST" action="{{ route('client.tickets.reply',$ticket) }}" class="mt-6 grid gap-3">@csrf<textarea class="field" name="message" required></textarea><button class="btn-primary w-fit">Yanıtla</button></form></div></x-layouts.panel>
