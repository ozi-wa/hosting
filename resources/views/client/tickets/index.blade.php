<x-layouts.panel title="Destek Talepleri"><div class="mb-4 flex justify-between"><h1 class="text-2xl font-bold">Destek Talepleri</h1><a class="btn-primary" href="{{ route('client.tickets.create') }}">Talep Aç</a></div><div class="panel">@foreach($tickets as $ticket)<a class="block border-b border-slate-100 py-3 dark:border-slate-800" href="{{ route('client.tickets.show',$ticket) }}">{{ $ticket->subject }} <span class="badge">{{ \App\Support\Status::label($ticket->status) }}</span></a>@endforeach{{ $tickets->links() }}</div></x-layouts.panel>
